# Requirements

- `docker`
- `docker-compose`

# Running

Clone this repo, then run the following command in the project directory root:

```shell
$ docker-compose up -d
```

The app will be listening on port `3333`

#### Note: There's an Insomnia workspace file included
